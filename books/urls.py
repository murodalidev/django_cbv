from django.urls import path
# from .views import IndexView, BookDetailView

app_name = 'books'

urlpatterns = [
    # path('', IndexView.as_view(), name='books-list'),
    # path('<slug:slug>/', BookDetailView.as_view(), name='books-detail'),
]
